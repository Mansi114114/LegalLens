# Extra dependencies for the offline scripts only — question_train.py and
# check_labels.py. Deliberately kept out of requirements.txt so the deployed
# image doesn't carry pandas, which the web service never imports.
#
#   pip install -r requirements.txt -r requirements-dev.txt
pandas==2.3.3
